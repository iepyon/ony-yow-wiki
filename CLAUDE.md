# ONY-YOW Wiki — 規約

ONY-YOW（O:起きていること / N:望んでいること / Y:やってみること / W:分かったこと）を
記録し、昇格させ、レビューする LLM Wiki。AI はこのファイルの規約に従って振る舞う。

**このファイルには、ここにしか無い規約だけを書く。**
Markdown・スライド・リンク・バンドルの記法の正本は
`/Users/eiji/src/claude-skills/slide-wiki/`（SKILL.md と ontology.md）。
ONY-YOW の定義の正本は [origin/summary.md](origin/summary.md)（原典の全251ページ要約）。
そちらにある内容をここへ写さない。

## 3層

| 層 | 場所 | 編集権 |
|---|---|---|
| origin（不変層） | `origin/` | 変更しない。原典の読解資産 |
| journal（一次記録） | `journal/` | `/ony` `/yow` または手書きで**追記のみ**。過去行の編集禁止 |
| wiki（昇格層・OKF バンドル） | `wiki/` | `/promote` が PR 経由で追加。`index.md` と `log.md` は生成物（`gen-okf-index.ts`） |

**「ログ」という語を2つの意味で使わない**:
`wiki/log.md` は**バンドル更新履歴**（機械が生成）、`journal/` は**本人の全件1行ログ**（一次記録）。

## journal の行形式

```
- <YYYY-MM-DDTHH:MM> #<週内連番> <ony|yow> <6活動> | <要素を / 区切り>
```

例:
```
- 2026-08-14T09:12 #03 ony 計画 | O:朝会で3人が同じ質問をした / N:仕様書だけで判断できる状態 / Y:仕様書にFAQ節を書く / O2:翌週の朝会で同じ質問が出ない
- 2026-08-14T17:40 #03 yow 検証 | Y:FAQ節を書いた / O:質問は来ずSlackで別の質問が出た / W:質問は所在ではなく例の不足から出ていた
```

- 要素キー: `O:` `N:` `Y:` `O2:` `W:` を ` / ` で区切る。ony 行は O/N/Y（O2/W は任意）、yow 行は Y/O/W
- **`#NN` は ONY 行と YOW 行で共有する**（Y が2つの三つ組の接続点 — summary.md §2.1）。`grep '#03'` で対が取れる
- **O2（起きそうなこと）は任意だが、無いと後知恵の検査が効かない**
- **過去行は編集しない。** 強制フックは無い。担保は git 履歴と `/onyw-review` の改変検査（`git log -p journal/`）
- **ony 行と yow 行は別コミットにする**（同一コミットだと「O2 を結果を知る前に書いた」ことを
  履歴が証明できない — PR #1 のレビューで判明）。ony を書いたら都度コミットするのが最も強い
- 手書き追記も正規の記録手段（1〜2分以内が目安。原典 p160）

## 6活動の語彙（summary.md §5）

`観察 / 仮説立案 / 実行 / 検証 / 計画 / 提案` — journal 行とスライドの `tags` に使う。

## 引き算診断（summary.md §6・原典 p150）

診断は **`(活動, 欠落要素) → 名前` の表引き**。欠落だけでは決まらない
（`{O,N,Y}` は完全な計画でも、W を欠いた実行でもある）。

| 活動 | 欠落 | 診断名 |
|---|---|---|
| 観察 | N | 見ただけ |
| 仮説立案 | W | ただの予測 |
| 実行 | W | 作業をこなすだけ |
| 計画 | O | 机上の空論 |
| 提案 | 相手の O・N | 押し売り |
| 提案 | 自分の O・N・Y | 御用聞き |

診断は**分類であって禁止ではない**（原典 p152「要素を足したり外したりしてスキルの性質を変える」）。
名前を返すだけで、記録を止めない。

## 週とファイル名

- 週は **ISO 週**（月曜開始）。`2026-W33` 形式
- journal は `journal/<人>-<年>-W<週>.md`、デッキは `wiki/<人>-<年>-W<週>.md`（同名・別ディレクトリ）
- スライド ID は `<!--id:YYYYMMDD-NN-->`。**日付部は ony 行を書いた日**、`NN` は journal の `#NN` と一致させる
  （`/onyw-review` の後知恵検査がこの対応に依存する）
- `wiki/order.yaml` の月グループは**その週の月曜が属する月**
- デッキの `status` は週の途中は `draft`、週が明けたら `stable`

## スライドの形（フェーズ1 は `<!--grid:3x2-->`）

セルは**行優先**で埋まる（slide-wiki ontology.yaml の Grid 宣言）。
記述順は `O → O2 → N → W → Y → O(実測)`、見た目は:

| | 左列（ONY） | 右列 |
|---|---|---|
| 行1 | O 起きていること | O2 起きそうなこと |
| 行2 | N 望んでいること | W 分かったこと |
| 行3 | Y やってみること | O 起きたこと |

記述順と読み順が食い違うので、**スライドは手で書かず `/promote` に生成させる**。
1スライド1000字上限・目安240字。frontmatter の `type` は `deck`
（独自型は `--lint --strict` が弾く。ONY-YOW デッキであることは `tags` の `ony-yow` で表す）。

## ワークフロー

| やりたいこと | 手段 |
|---|---|
| ONY を記録する（計画・提案・観察） | `/ony` または journal へ手書き1行 |
| YOW を記録する（検証・振り返り） | `/yow` または手書き1行 |
| 価値が分かった対をスライドへ昇格 | `/promote <年>-W<週>#<NN>` → PR |
| PR にレビューコメント | `/onyw-review <PR番号>`（バイアス / 因果の弱さ / 別の選択肢 の3観点） |
| 一覧・バックリンクを見る | `npx tsx <slide-wiki>/assets/src/cli.ts --wiki wiki out/index.html` |

**昇格 = PR。マージ = 昇格の確定。** レビューの往復は PR コメントに残す（A3 のキャッチボール）。
可視性は場所で表す: journal（本人のみ）→ PR（レビュー中）→ merge 済み wiki（共有）。

## W に規律を課さない

W（分かったこと）は自由記述。falsifier・確信度・証拠の階梯を要求しない（原典忠実の決定）。
レビューで因果の弱さを**問う**ことはするが、記入を**block しない**。

## 記述言語

すべて日本語。要素キー（O/N/Y/O2/W）・ID・frontmatter キーは原文のまま。
