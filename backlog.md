# backlog — ony-yow 自体の改善

ony-yow を使っていて見つかった不満と、その次の一歩。**記録（カード）ではない**ので
日次 PR には載せず、main に直コミットする（CLAUDE.md「日次 PR に載せるのは記録だけ」）。

## 書き方

項目には **O（何が起きているから）を必ず書く**。引き算診断の
「〈計画〉で O が空 → 机上の空論」（p150）をバックログ側で先に防ぐため。
優先度スコア（ICE/PXL）は付けない — 項目が10件を超えるまでは過剰で、
「次にやる1件」が決まれば足りる。

```markdown
- [ ] 何を（1行）
      O: どのカード / どの観測から来たか
      次の一歩: 一番小さい着手
```

## 並べる基準

目的は**個人の仮説検証の練習量**（`20260814-04` の N =「続けられる」。
原典 3000/年 ≒ 8/日 に対して現状 1〜2/日）。したがって順序は:

1. **1ループあたりのコストを下げるか**（下がれば回数が増える）
2. **明日の朝そのまま Y にできるか**（着手の小ささ）
3. **観測された O があるか**

記録の質（後知恵防止・因果の検査）と見える化は、**回数が増えてからでないと効かない**ので後ろ。

---

## 今週やる

- [ ] 対話の定型文を削る（6活動は直近の activity と Y の文面から1語を推定提示して確認だけにする／
      `/yow` の「O2 を逐語で見せる・一致判定は本人」の前置きは初回だけにする）
      O: `20260815-01` の O2「同じことを聞かれる。同じ質問がある。冗長なところがある」
      次の一歩: `.claude/skills/ony/SKILL.md` 手順3 と `.claude/skills/yow/SKILL.md` 手順3 の文言修正だけ

- [ ] `/ony` の冒頭で `backlog.md` の未着手項目を出し、そこから今日の Y を選べるようにする
      O: 「今日やることが思いつかない日」はループが0になる。バックログを作った意味をその日のうちに回収する
      次の一歩: `.claude/skills/ony/SKILL.md` 手順2 に1行

- [ ] 前日 YOW の「O 起きたこと / W 分かったこと」を、今日の O 候補として提示する
      O: O をゼロから聞く分のコストが毎朝乗る。原典の「Y が2つの三つ組の接続点」（p108）を朝側で実装する形になる
      次の一歩: `tools/oylint.py` に直近の YOW を出すオプション → `/ony` 手順2 から呼ぶ

## 次（今週の3件を回した後）

- [ ] `tools/new_card.py` — ID 採番・日付取得・ブランチ確認を機械に追い出す
      O: `/ony` 手順1・4 の定型作業が対話に混ざっている
      次の一歩: 効くが、まず対話の言葉を削る方が安い。3件を回した後で再評価する

- [ ] `/ony` に `relates` を聞く動線を足す（未検証ループ一覧から候補提示）
      O: `ontology.yaml` に `relates` があるのに `/ony` の手順に聞く場所がない（`20260815-01` は手で付けた）
      次の一歩: 型と手順の乖離の修正。コスト削減ではないので今週ではない

- [ ] `oylint --pending` に経過日数を出し、N日超で warning
      O: いまの `--pending` は一覧するだけで滞留が見えない
      次の一歩: 未検証が3件を超えてから。1件の今は効かない

- [ ] slide-wiki の GitHub Pages 公開
      O: `20260814-04` の W が残した宿題（public 化は済んだが公開自体は未着手）
      次の一歩: 一人で見る間は PR 本文の表とデッキへのリンクで足りているので、次点

## 貯めておく（量が増えてから意味が出る）

- [ ] 週サマリー「今週の数字」に目標ループ数の線を出す（8/日 との対比）
      O: 現状 1〜2/日。目標との差が数字で見えていない
      次の一歩: ループが日常的に複数出るようになってから

- [ ] W カタログの週横断インデックス
      O: いまの W カタログは週デッキ内で閉じている
      次の一歩: W が10件を超えてから

- [ ] バックログ項目 → 生まれたカード ID の書き戻し
      O: 消化した項目がどのループになったか追えない
      次の一歩: 項目が実際に消化され始めてから

## 落とす（いま入れない — 理由ごと残す）

- **CI（GitHub Actions で oylint / gen_deck --check）** — 一人運用で毎回手元で叩いている。二重になる
- **ONY 凍結の機械検査** — ファイル分離ですでに構造的に担保されている。
  自分を騙す動機が薄い場面に検査を足すのは引き算の逆
- **ICE / PXL スコアリング** — 10件未満では過剰、というのが調査の一致した見解。
  「次にやる1件」を決めれば足りる

---

## 別枠: 決めごと（タスクではなく規約の合意）

- **`origin/` が gitignore の扱い** — クラウド/別マシンのセッションでは規約の根拠（p番号）が引けない。
  `CLAUDE.md` と `ontology.yaml` が SSoT として指しているものが読めない環境をどう扱うか
- **O2 の確信度 / Brier score・calibration** — O2→O の突き合わせとは相性が良いが、
  `CLAUDE.md`「W に規律を課さない（falsifier・確信度・証拠の階梯を要求しない）」と正面衝突する。
  入れるなら規約変更の合意が先
